(function () { 'use strict'; if (customElements.get('smart-clock')) return; var SmartClock = (function () { function SmartClock() { var self = Reflect.construct(HTMLElement, [], SmartClock); self._interval = null; self._timezone = 'UTC'; return self; } SmartClock.prototype = Object.create(HTMLElement.prototype); SmartClock.prototype.constructor = SmartClock; SmartClock.prototype.connectedCallback = function () { this._timezone = this.getAttribute('timezone') || 'UTC'; this._render(); this._loadPreferenceIndicator(); }; SmartClock.prototype.disconnectedCallback = function () { if (this._interval) { clearInterval(this._interval); this._interval = null; } }; SmartClock.prototype._render = function () { var self = this; this.innerHTML = '<span class="smart-clock-time" aria-live="polite" aria-label="Current time in ' + this._escapeAttr(this._timezone) + '">' + this._getCurrentTime() + '</span>' + '<span class="smart-clock-label">' + this._getDisplayName() + '</span>'; if (this._interval) clearInterval(this._interval); this._interval = setInterval(function () { var timeEl = self.querySelector('.smart-clock-time'); if (timeEl) { timeEl.textContent = self._getCurrentTime(); timeEl.setAttribute('aria-label', 'Current time in ' + self._escapeAttr(self._timezone)); } }, 1000); }; SmartClock.prototype._getCurrentTime = function () { try { return new Date().toLocaleTimeString('en-US', { timeZone: this._timezone, hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: true }); } catch (e) { return '--:--:--'; } }; SmartClock.prototype._getDisplayName = function () { try { if (typeof GTZL10N !== 'undefined' && GTZL10N.getTimezoneDisplayName) { return GTZL10N.getTimezoneDisplayName(this._timezone); } } catch (e) { } return this._timezone; }; SmartClock.prototype._loadPreferenceIndicator = function () { var self = this; try { if (typeof preferenceService !== 'undefined') { var prefs = preferenceService.get(); if (prefs && prefs.timezone === self._timezone) { var indicator = document.createElement('span'); indicator.className = 'local-indicator'; indicator.textContent = ' (Local)'; indicator.title = 'Identified from your preferences'; indicator.style.cssText = 'font-size:0.65rem;color:#10b981;font-weight:600;margin-left:4px;'; self.appendChild(indicator); } } } catch (e) { } }; SmartClock.prototype._escapeAttr = function (value) { return String(value) .replace(/&/g, '&amp;') .replace(/"/g, '&quot;') .replace(/</g, '&lt;') .replace(/>/g, '&gt;'); }; Object.defineProperty(SmartClock, 'observedAttributes', { get: function () { return ['timezone']; } }); SmartClock.prototype.attributeChangedCallback = function (name, oldVal, newVal) { if (name === 'timezone' && oldVal !== newVal) { this._timezone = newVal; this._render(); } }; return SmartClock; })(); customElements.define('smart-clock', SmartClock); })();