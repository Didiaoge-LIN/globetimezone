(function (global) { 'use strict'; function PrefetchStrategy() { this._observer = null; this._prefetched = {}; } PrefetchStrategy.prototype.observeLinks = function () { if (!('IntersectionObserver' in window)) { this._prefetchAll(); return; } var self = this; this._observer = new IntersectionObserver( function (entries) { for (var i = 0; i < entries.length; i++) { if (entries[i].isIntersecting) { self._handleObserved(entries[i].target); } } }, { rootMargin: '100px' } ); var links = document.querySelectorAll('a[data-prefetch]'); for (var j = 0; j < links.length; j++) { this._observer.observe(links[j]); } }; PrefetchStrategy.prototype._handleObserved = function (el) { var href = el.getAttribute('href'); if (!href || this._prefetched[href]) return; var link = document.createElement('link'); link.rel = 'prefetch'; link.href = href; link.as = el.getAttribute('data-prefetch-as') || 'document'; document.head.appendChild(link); this._prefetched[href] = true; if (this._observer) { this._observer.unobserve(el); } }; PrefetchStrategy.prototype.prefetchOnIdle = function (url) { if (!url || this._prefetched[url]) return; var self = this; var doFetch = function () { try { fetch(url, { priority: 'low', method: 'GET' }) .then(function () { self._prefetched[url] = true; }) .catch(function () { }); } catch (e) { } }; if ('requestIdleCallback' in window) { window.requestIdleCallback(doFetch, { timeout: 2000 }); } else { setTimeout(doFetch, 2000); } }; PrefetchStrategy.prototype._prefetchAll = function () { var links = document.querySelectorAll('a[data-prefetch]'); for (var i = 0; i < links.length; i++) { var href = links[i].getAttribute('href'); if (href && !this._prefetched[href]) { this.prefetchOnIdle(href); } } }; PrefetchStrategy.prototype.prefetchCritical = function () { var critical = [ '/world-map.html', '/countdown.html', '/holidays.html' ]; var self = this; for (var i = 0; i < critical.length; i++) { self.prefetchOnIdle(critical[i]); } }; PrefetchStrategy.prototype.destroy = function () { if (this._observer) { this._observer.disconnect(); this._observer = null; } this._prefetched = {}; }; var instance = new PrefetchStrategy(); if (typeof module !== 'undefined' && module.exports) { module.exports = instance; } global.prefetchStrategy = instance; })(typeof window !== 'undefined' ? window : globalThis);