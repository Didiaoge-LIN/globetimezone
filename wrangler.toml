name = "globetimezone"
type = "static"
compatibility_date = "2026-06-01"

# 静态网站构建配置（Cloudflare Pages）
# 通过 GitHub 连接时，Pages 会自动读取此文件

[build]
command = ""
publish = "public"

# 纯静态站点，无需构建命令

# 环境变量（如需要）
#[vars]
# API_BASE_URL = "https://globetimezone.com"
