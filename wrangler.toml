name = "globetimezone"
compatibility_date = "2026-06-01"

# Cloudflare Pages 构建配置
# 纯静态 + Pages Functions 混合模式

[build]
command = ""
publish = "."

# KV Namespace 绑定
# AD_KV: 广告解锁权益体系专用（速率限制+日限持久化）
# 已创建: 2026-07-05, ID: 9a5a7390c4c04610ac495730da91ee26
# ⚠️ CF Pages 的 KV 绑定需在 Dashboard 配置，不是 wrangler.toml:
#    Pages → globetimezone-web → Settings → Functions → KV namespace bindings
#    Variable name: AD_KV  |  KV namespace: AD_KV

# 环境变量（如需要）
#[vars]
# API_BASE_URL = "https://globetimezone.com"
